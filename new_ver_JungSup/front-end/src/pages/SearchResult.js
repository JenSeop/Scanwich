import React, {useEffect} from 'react';
import { Typography, Grid, AccordionSummary, AccordionDetails, Paper, Tooltip } from '@mui/material';
import SearchIcon from '@mui/icons-material/Search';

const SearchResult = ({}) => {
  return (
    <>
    </>
  );
};

export default SearchResult;
